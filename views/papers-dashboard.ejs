<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <title>論文一覧と管理</title>
    <style>
      body {
        font-family: sans-serif;
        margin: 20px;
      }
      h1,
      h2 {
        color: #333;
      }
      ul {
        list-style: none;
        padding: 0;
      }
      li {
        margin-bottom: 5px;
      }
      form {
        margin-bottom: 20px;
        padding: 15px;
        border: 1px solid #ddd;
        border-radius: 5px;
      }
      form input[type="text"],
      form input[type="url"],
      form textarea {
        width: calc(100% - 22px);
        padding: 8px;
        margin-bottom: 10px;
        border: 1px solid #ccc;
        border-radius: 3px;
      }
      form button {
        padding: 10px 15px;
        background-color: #007bff;
        color: white;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        margin-right: 5px; /* ボタン間のスペース */
      }
      form button:hover {
        background-color: #0056b3;
      }
      table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 20px;
      }
      th,
      td {
        border: 1px solid #ddd;
        padding: 8px;
        text-align: left;
      }
      th {
        background-color: #f2f2f2;
      }
      .search-form label {
        margin-right: 10px;
      }
      .search-form input {
        margin-right: 10px;
      }
      .header-section {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 20px;
      }
      .header-section button {
        background-color: #dc3545;
      }
      .header-section button:hover {
        background-color: #c82333;
      }
    </style>
  </head>
  <body>
    <div class="header-section">
      <h1>論文一覧と管理</h1>
      <form action="/logout" method="get">
        <button type="submit">ログアウト</button>
      </form>
    </div>

    <h2>論文検索</h2>
    <form action="/papers-dashboard" method="get" class="search-form">
      <label for="searchTitle">タイトル:</label>
      <input
        type="text"
        id="searchTitle"
        name="searchTitle"
        value="<%= searchTitle %>"
        placeholder="タイトルで検索"
      />

      <label for="searchAuthor">著者:</label>
      <input
        type="text"
        id="searchAuthor"
        name="searchAuthor"
        value="<%= searchAuthor %>"
        placeholder="著者名で検索"
      />

      <label for="searchCategory">カテゴリ:</label>
      <input
        type="text"
        id="searchCategory"
        name="searchCategory"
        value="<%= searchCategory %>"
        placeholder="カテゴリで検索"
      />
      <button type="submit">検索</button>
      <button type="button" onclick="location.href='/papers-dashboard'">
        リセット
      </button>
    </form>

    ---

    <h2>論文一覧</h2>
    <% if (papers.length > 0) { %>
    <table>
      <thead>
        <tr>
          <th>タイトル</th>
          <th>著者名</th>
          <th>カテゴリ</th>
          <th>ウェブサイト</th>
          <th>コメント</th>
        </tr>
      </thead>
      <tbody>
        <% papers.forEach(paper => { %>
        <tr>
          <td><%= paper.name %></td>
          <td><%= paper.author %></td>
          <td><%= paper.category || 'N/A' %></td>
          <td>
            <% if (paper.url) { %>
            <a href="<%= paper.url %>" target="_blank" rel="noopener noreferrer"
              >リンク</a
            >
            <% } else { %> N/A <% } %>
          </td>
          <td><%= paper.comment || 'N/A' %></td>
        </tr>
        <% }) %>
      </tbody>
    </table>
    <% } else { %>
    <p>
      論文はまだ登録されていません、または検索条件に一致する論文が見つかりませんでした。
    </p>
    <% } %> ---

    <h2>論文追加</h2>
    <form action="/papers" method="post">
      <label for="paperName">論文タイトル (必須):</label>
      <input
        type="text"
        id="paperName"
        name="name"
        placeholder="論文タイトルを入力"
        required
      />

      <label for="paperAuthor">著者名 (必須):</label>
      <input
        type="text"
        id="paperAuthor"
        name="author"
        placeholder="著者名を入力"
        required
      />

      <label for="paperCategory">カテゴリ (任意):</label>
      <input
        type="text"
        id="paperCategory"
        name="category"
        placeholder="カテゴリを入力"
      />

      <label for="paperUrl">ウェブサイトURL (任意):</label>
      <input
        type="url"
        id="paperUrl"
        name="url"
        placeholder="論文のURLを入力"
      />

      <label for="paperComment">コメント (任意):</label>
      <textarea
        id="paperComment"
        name="comment"
        placeholder="コメントを入力"
        rows="4"
      ></textarea>

      <button type="submit">論文追加</button>
    </form>
  </body>
</html>
